"""Общий маркер приватности конструктора для value objects этого пакета
(ProductId) — та же схема, что и `_PRIVATE_MARKER` у `kernel_domain.Entity`,
но у VO нет общего базового `__init__`, который централизованно бы это
обеспечивал, поэтому собственный `__init__` каждого VO сверяется с этим
единым токеном."""

_PRIVATE_MARKER = object()
