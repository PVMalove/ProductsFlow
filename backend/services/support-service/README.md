# support-service

Independently deployable support API scaffold. The service follows the
canonical layout in repository ADR 0026 and is ready for the support phase.

Run checks and tests from `backend/` with `make check pkg=support-service`
and `make test pkg=support-service`.
