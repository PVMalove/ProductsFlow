services:
  support-api:
    build:
      context: ../..
      dockerfile: services/support-service/Dockerfile
    environment:
      APP_ENV: ${APP_ENV:-dev}
      SUPPORT_DATABASE_URL: ${SUPPORT_DATABASE_URL:-}
