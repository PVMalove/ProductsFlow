# ruff: noqa: E501
# Сгенерировано `vulture --make-whitelist .` — ложные срабатывания, чтобы
# vulture молчал.
# TokenVerifier.verify_token/FakeTokenVerifier.verify_token — сигнатура
# метода Protocol/test-double, параметр намеренно не используется в теле
# заглушки.
token = None
__all__ = ["token"]
