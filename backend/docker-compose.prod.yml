services:
  identity-api:
    environment:
      APP_ENV: prod
    ports:
      - "9013:8000"
    restart: unless-stopped

  catalog-api:
    environment:
      APP_ENV: prod
    ports:
      - "9014:8000"
    restart: unless-stopped

  support-api:
    environment:
      APP_ENV: prod
    ports:
      - "9015:8000"
    restart: unless-stopped
