[project]
name = "kernel-platform"
version = "0.1.0"
description = "Инфраструктурные примитивы ProductsFlow (TokenVerifier/IdentityClient/Outbox-Inbox/bootstrap) — httpx/SQLAlchemy/OTEL допустимы (ADR 0013)."
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kernel_platform"]
