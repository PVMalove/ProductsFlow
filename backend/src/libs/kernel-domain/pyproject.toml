[project]
name = "kernel-domain"
version = "0.1.0"
description = "Общие доменные примитивы ProductsFlow (Result/Error/Entity/DomainEvent) — только stdlib (ADR 0013)."
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kernel_domain"]
